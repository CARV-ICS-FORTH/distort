---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nvmepartitions.storage.distort.io
spec:
  group: storage.distort.io
  names:
    kind: NVMePartition
    listKind: NVMePartitionList
    plural: nvmepartitions
    singular: nvmepartition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Partition Size
      jsonPath: .spec.size
      name: Size
      type: string
    - description: Assigned Node
      jsonPath: .spec.nodeName
      name: Node
      type: string
    - description: Current State
      jsonPath: .status.state
      name: State
      type: string
    - description: NVMe-oF NQN
      jsonPath: .status.nqn
      name: NQN
      type: string
    - description: Portal IP
      jsonPath: .status.portalIP
      name: Portal
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NVMePartition is the Schema for the nvmepartitions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NVMePartition
            properties:
              accessMode:
                description: AccessMode indicates the PVC access mode (e.g., ReadWriteOnce,
                  ReadOnlyMany).
                type: string
              claimRef:
                description: |-
                  ClaimRef identifies the exact live claim that authorized this allocation.
                  Populated by the management controller together with placement fields.
                properties:
                  name:
                    description: Name is the name of the claim.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the namespace containing the claim.
                    minLength: 1
                    type: string
                  uid:
                    description: UID is the immutable Kubernetes UID of the claim.
                    type: string
                required:
                - name
                - namespace
                - uid
                type: object
                x-kubernetes-validations:
                - message: claimRef is immutable
                  rule: self == oldSelf
                - message: claimRef UID must not be empty
                  rule: size(self.uid) > 0
              nodeName:
                description: |-
                  NodeName is the node where this partition should be created.
                  Often left empty by the CSI provisioner and populated by the mutating scheduler.
                type: string
                x-kubernetes-validations:
                - message: nodeName is immutable after assignment
                  rule: self == oldSelf
              parentDeviceSerialNumber:
                description: |-
                  ParentDeviceSerialNumber is the serial number of the NVMeDevice this partition is allocated from.
                  Populated by the mutating scheduler (Mgmt Controller).
                type: string
                x-kubernetes-validations:
                - message: parentDeviceSerialNumber is immutable after assignment
                  rule: self == oldSelf
              size:
                anyOf:
                - type: integer
                - type: string
                description: Size is the requested capacity for the volume.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              targetBackend:
                default: spdk
                description: TargetBackend specifies the export technology (e.g.,
                  "spdk" or "kernel").
                enum:
                - spdk
                - kernel
                type: string
              targetOptions:
                additionalProperties:
                  type: string
                description: TargetOptions provides backend-specific customization
                  flags (e.g., spdk core masks).
                maxProperties: 1
                type: object
                x-kubernetes-validations:
                - message: targetOptions contains an unsupported backend option
                  rule: self.all(k, k == 'spdk-core-mask')
                - message: spdk-core-mask must be at most 258 characters and match
                    0x followed by hexadecimal digits
                  rule: '!(''spdk-core-mask'' in self) || (size(self[''spdk-core-mask''])
                    <= 258 && self[''spdk-core-mask''].matches(''^0x[0-9A-Fa-f]+$''))'
              volumeManager:
                default: partition
                description: VolumeManager specifies the volume manager (e.g., "partition"
                  or "lvm").
                enum:
                - partition
                - lvm
                type: string
            required:
            - size
            type: object
            x-kubernetes-validations:
            - message: nodeName, parentDeviceSerialNumber, and claimRef must either
                all be omitted or all be set by the scheduler
              rule: (!has(self.nodeName) && !has(self.parentDeviceSerialNumber) &&
                !has(self.claimRef)) || (has(self.nodeName) && has(self.parentDeviceSerialNumber)
                && has(self.claimRef))
            - message: spdk-core-mask is supported only by the spdk backend
              rule: '!has(self.targetOptions) || !(''spdk-core-mask'' in self.targetOptions)
                || !has(self.targetBackend) || self.targetBackend == ''spdk'''
          status:
            description: status defines the observed state of NVMePartition
            properties:
              backendVolumeID:
                description: BackendVolumeID is the block path or logical-volume identity
                  returned by the volume manager.
                type: string
              conditions:
                description: Conditions represent the current state of the NVMePartition
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              externalID:
                description: ExternalID is the immutable, globally unique identifier
                  used for backend resources.
                type: string
              nqn:
                description: NQN is the NVMe Qualified Name generated by the agent
                  upon successful export.
                type: string
              portalIP:
                description: PortalIP is the RDMA IP address for the CSI node side
                  to connect to.
                type: string
              portalPort:
                description: 'PortalPort is the RDMA port (default: 4420).'
                type: integer
              spdkBaseBdev:
                description: SPDKBaseBdev is the exact SPDK namespace bdev backing
                  the logical volume.
                type: string
              spdkLvolName:
                description: SPDKLvolName is the logical volume name within its store.
                type: string
              spdkLvolUUID:
                description: SPDKLvolUUID is the immutable UUID of the logical volume
                  bdev.
                type: string
              spdkLvstoreName:
                description: SPDKLvstoreName is the logical volume store name used
                  during provisioning.
                type: string
              spdkLvstoreUUID:
                description: SPDKLvstoreUUID is the immutable UUID of the logical
                  volume store.
                type: string
              state:
                default: Pending
                description: State is the current status of the partition creation/export.
                enum:
                - Pending
                - Creating
                - Exported
                - Failed
                type: string
              volumeID:
                description: VolumeID is the opaque CSI handle identifying this exact
                  namespaced object and UID.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
