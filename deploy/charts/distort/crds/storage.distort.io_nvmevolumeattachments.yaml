---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nvmevolumeattachments.storage.distort.io
spec:
  group: storage.distort.io
  names:
    kind: NVMeVolumeAttachment
    listKind: NVMeVolumeAttachmentList
    plural: nvmevolumeattachments
    singular: nvmevolumeattachment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NVMeVolumeAttachment is the durable single-writer fencing record
          for a volume.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired attachment owner.
            properties:
              attachmentID:
                description: AttachmentID distinguishes this attachment lifetime from
                  every retry or takeover.
                minLength: 1
                type: string
              hostNQN:
                description: HostNQN is the deterministic NVMe host identity authorized
                  by the target.
                pattern: ^nqn\.2026-01\.io\.distort:host-[a-f0-9]{32}$
                type: string
              nodeID:
                description: NodeID is the CSI node which exclusively owns the attachment.
                maxLength: 253
                minLength: 1
                type: string
              volumeRef:
                description: VolumeRef identifies the exact partition being attached.
                properties:
                  name:
                    description: Name is the partition name in the attachment namespace.
                    minLength: 1
                    type: string
                  uid:
                    description: UID prevents a deleted partition name from authorizing
                      its replacement.
                    minLength: 1
                    type: string
                required:
                - name
                - uid
                type: object
            required:
            - attachmentID
            - hostNQN
            - nodeID
            - volumeRef
            type: object
            x-kubernetes-validations:
            - message: attachment ownership is immutable; delete and recreate the
                attachment to change nodes
              rule: self == oldSelf
          status:
            description: Status reports whether the provider target has applied the
              attachment.
            properties:
              conditions:
                description: Conditions report whether target access is ready or being
                  revoked.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedAttachmentID:
                description: ObservedAttachmentID is set only after the provider target
                  authorizes HostNQN.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
