---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nvmedeviceclaims.storage.distort.io
spec:
  group: storage.distort.io
  names:
    kind: NVMeDeviceClaim
    listKind: NVMeDeviceClaimList
    plural: nvmedeviceclaims
    singular: nvmedeviceclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Target Serial Number
      jsonPath: .spec.serialNumber
      name: SerialNumber
      type: string
    - description: Is Active
      jsonPath: .status.active
      name: Active
      type: boolean
    - description: Matched Device
      jsonPath: .status.matchedDevice
      name: MatchedDevice
      type: string
    - description: Node
      jsonPath: .status.nodeName
      name: Node
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NVMeDeviceClaim is the Schema for the nvmedeviceclaims API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NVMeDeviceClaim
            properties:
              serialNumber:
                description: |-
                  SerialNumber is the exact hardware serial number of the NVMe device to claim.
                  This uniquely identifies the drive even if it moves to another node or PCIe slot.
                type: string
            required:
            - serialNumber
            type: object
          status:
            description: status defines the observed state of NVMeDeviceClaim
            properties:
              active:
                default: false
                description: Active indicates whether the claim has successfully bound
                  to a matching NVMeDevice.
                type: boolean
              conditions:
                description: Conditions represent the current state of the NVMeDeviceClaim
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              matchedDevice:
                description: MatchedDevice is the name of the NVMeDevice resource
                  that this claim is bound to.
                type: string
              nodeName:
                description: NodeName is the node where the matched device currently
                  resides.
                type: string
            required:
            - active
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
