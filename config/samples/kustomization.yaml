## Append samples of your project ##
resources:
# NVMeDevice and RDMAStorageNode are agent-owned discovery resources. Their
# example files document shape only and are deliberately not applied here.
- storage_v1alpha1_nvmedeviceclaim.yaml
- storage_v1alpha1_nvmepartition.yaml
# +kubebuilder:scaffold:manifestskustomizesamples
