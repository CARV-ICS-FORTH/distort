apiVersion: storage.distort.io/v1alpha1
kind: NVMeDeviceClaim
metadata:
  labels:
    app.kubernetes.io/name: distort
    app.kubernetes.io/managed-by: kustomize
  name: nvmedeviceclaim-sample
spec:
  # Replace this value with an exact serial from `kubectl get nvmedevices`.
  serialNumber: REPLACE-WITH-A-DISCOVERED-SERIAL
