---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: rdmastoragenodes.storage.distort.io
spec:
  group: storage.distort.io
  names:
    kind: RDMAStorageNode
    listKind: RDMAStorageNodeList
    plural: rdmastoragenodes
    singular: rdmastoragenode
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Node Name
      jsonPath: .spec.nodeName
      name: Node
      type: string
    - description: RDMA Portal IP
      jsonPath: .spec.rdmaIP
      name: RDMA IP
      type: string
    - description: Transport Type
      jsonPath: .spec.transport
      name: Transport
      type: string
    - description: Available Storage
      jsonPath: .status.freeCapacity
      name: Free Cap
      type: string
    - description: Active Subsystems
      jsonPath: .status.activeExports
      name: Exports
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RDMAStorageNode is the Schema for the rdmastoragenodes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RDMAStorageNode
            properties:
              linkSpeed:
                description: LinkSpeed is the speed of the RDMA link (e.g., "100Gbps").
                type: string
              nodeName:
                description: NodeName is the name of the Kubernetes node.
                type: string
              rdmaIP:
                description: |-
                  RDMAIP is the IP address bound to the RDMA-capable NIC interface.
                  This is the IP that the CSI nodes will connect to.
                type: string
              transport:
                description: Transport is the active RDMA transport type (e.g., RoCEv2,
                  InfiniBand).
                enum:
                - RoCEv2
                - InfiniBand
                - TCP
                type: string
            required:
            - nodeName
            - rdmaIP
            - transport
            type: object
          status:
            description: status defines the observed state of RDMAStorageNode
            properties:
              activeExports:
                description: ActiveExports is the number of active NVMe-oF subsystems
                  currently exported by this node.
                type: integer
              conditions:
                description: Conditions represent the current state of the RDMAStorageNode
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              freeCapacity:
                anyOf:
                - type: integer
                - type: string
                description: |-
                  FreeCapacity is the sum of free capacities across all Claimed NVMeDevices on this node.
                  Used by the Mgmt-Controller to schedule new NVMePartitions.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              totalCapacity:
                anyOf:
                - type: integer
                - type: string
                description: TotalCapacity is the sum of capacities of all Available
                  or Claimed NVMeDevices on this node.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
